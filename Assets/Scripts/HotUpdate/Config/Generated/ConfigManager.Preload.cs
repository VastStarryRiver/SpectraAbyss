// <auto-generated>
// 本文件由 VastStarryRiver/Config/导出Excel配置 生成，请勿手改
using Invariable;
using System;



namespace HotUpdate
{
    public static partial class ConfigManager
    {
        /// <summary>
        /// 预加载全部配置表 bytes（可选，用于加载界面预热）
        /// </summary>
        public static void PreloadAll(Action callBack)
        {
            int remain = 2;
            Action step = () =>
            {
                remain--;
                if (remain <= 0)
                {
                    callBack?.Invoke();
                }
            };
            GetPlayer((data) =>
            {
                if (data == null)
                {
                    GameLog.Error("预加载配置失败：Player");
                }

                step();
            });
            GetRoleRune((data) =>
            {
                if (data == null)
                {
                    GameLog.Error("预加载配置失败：RoleRune");
                }

                step();
            });
        }

        /// <summary>
        /// 清除全部配置表缓存
        /// </summary>
        public static void ClearAll()
        {
            ClearPlayer();
            ClearRoleRune();
        }
    }
}